#include <bits/stdc++.h>

using namespace std;

int main (int argc, char *argv[]) {
    
    int N; 
    cin >> N;
    int mont[N];

    for (int i = 0; i < N; i++) {
        cin >> mont[i];
    }

    for (int i = 0; i < N; i++) {
        if (mont[i]  && mont[i + 1]) {

        }
    }

    return 0;
}
