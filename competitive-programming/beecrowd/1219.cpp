#include <bits/stdc++.h>
#include <cmath>

using namespace std;

int main (int argc, char *argv[]) {
        
    long double a, b, c;
    long double p = (a + b + c) / 2;

    long double A = sqrt(p*(p - a)*(p - b)*(p - c));

    while (cin >> a >> b >> c) {
         
    }


    return 0;
}
